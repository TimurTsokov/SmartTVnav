import LanguageService from './LanguageService';

const russian = {
    buttons: {
        activate: 'Активировать'
    }
};

const ukrainian = {
    buttons: {
        activate: 'Активувати'
    }
};

export function lang() {
    
}